# Server
PORT=5001
NODE_ENV=production

# MongoDB
MONGO_URI=mongodb://<username>:<password>@localhost:27017/demo-app?authSource=admin

# JWT / Auth (if used)
JWT_SECRET=your_jwt_secret_here

# Other configs (if any APIs/keys are used)
